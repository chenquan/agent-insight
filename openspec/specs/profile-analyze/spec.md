## Purpose

Analyze pprof profile files to identify performance hotspots, calculate flat and cumulative metrics, and generate structured output for AI-assisted performance debugging.
## Requirements
### Requirement: Parse pprof protobuf file
The system SHALL parse pprof protobuf format files generated by `go tool pprof` and other compatible tools.

#### Scenario: Successfully parse valid CPU profile
- **WHEN** user provides a valid CPU profile file (.pb.gz)
- **THEN** system successfully parses the file and extracts all samples and locations

#### Scenario: Report parse errors
- **WHEN** user provides an invalid or corrupted profile file
- **THEN** system outputs a clear error message indicating the parse failure

#### Scenario: Handle large files
- **WHEN** user provides a profile file larger than 1GB
- **THEN** system uses streaming parsing to avoid excessive memory consumption

### Requirement: Detect profile type automatically
The system SHALL automatically detect the profile type (cpu, heap, goroutine, etc.) based on the profile metadata.

#### Scenario: Detect CPU profile
- **WHEN** user provides a CPU profile without specifying --type
- **THEN** system identifies the type as "cpu" and reports it in output

#### Scenario: Detect heap profile
- **WHEN** user provides a heap profile without specifying --type
- **THEN** system identifies the type as "heap" and reports it in output

#### Scenario: Override auto-detection
- **WHEN** user specifies --type cpu explicitly
- **THEN** system uses the specified type regardless of auto-detection

### Requirement: Calculate and sort hotspots
The system SHALL calculate flat and cumulative values for each function and sort them by default in descending flat value order.

#### Scenario: Top 15 hotspots by default
- **WHEN** user runs analyze without --top flag
- **THEN** system outputs the top 15 functions by flat value

#### Scenario: Custom top N
- **WHEN** user specifies --top 20
- **THEN** system outputs the top 20 functions by flat value

#### Scenario: Cumulative sort
- **WHEN** user specifies --cum flag
- **THEN** system outputs functions sorted by cumulative value in descending order

#### Scenario: Include percentages
- **WHEN** system outputs hotspot list
- **THEN** each entry includes flat%, flat, cum%, cum values with proper percentage formatting

### Requirement: Handle missing symbol information
The system SHALL gracefully handle profiles that lack function symbol information, using Location IDs and memory addresses as fallback.

#### Scenario: Profile with function symbols
- **WHEN** profile contains function information
- **THEN** system outputs function names and file names in hotspot list

#### Scenario: Profile without function symbols
- **WHEN** profile lacks function information but has Location IDs
- **THEN** system outputs Location IDs and memory addresses in hotspot list
- **AND** system includes module information from Mapping when available

#### Scenario: Mixed symbol availability
- **WHEN** profile contains both symbolized and non-symbolized locations
- **THEN** system uses function names where available and Location IDs where missing
- **AND** JSON output maintains consistent structure with null for missing fields

#### Scenario: Inform user about symbolization
- **WHEN** profile has minimal symbol information
- **THEN** summary mentions limited symbol availability
- **AND** system provides suggestion for including debug symbols in future profiles

### Requirement: Handle multiple value types
The system SHALL correctly handle profiles with multiple value types (e.g., heap profiles with objects and bytes, Go heap profiles with four value types).

#### Scenario: Single value type (CPU profile)
- **WHEN** analyzing CPU profile with single value type
- **THEN** system uses the cpu_nanoseconds value for analysis

#### Scenario: Dual value types (standard heap profile)
- **WHEN** analyzing heap profile with objects and bytes values
- **THEN** system defaults to using bytes value for analysis
- **AND** JSON output includes both objects and bytes values

#### Scenario: Multiple value types (Go heap profile)
- **WHEN** analyzing Go heap profile with four value types (alloc_objects, alloc_space, inuse_objects, inuse_space)
- **THEN** system defaults to using inuse_space for analysis
- **AND** JSON output includes all four value types for complete information

#### Scenario: User-specified value type
- **WHEN** user specifies --value-type alloc_objects
- **THEN** system uses the specified value type for analysis instead of default
- **AND** output clearly indicates which value type is being used

#### Scenario: Value type not found
- **WHEN** user specifies --value-type that doesn't exist in the profile
- **THEN** system outputs error listing available value types from SampleType

### Requirement: Filter by function name patterns
The system SHALL support filtering functions using regular expressions for both inclusion and exclusion.

#### Scenario: Focus on specific functions
- **WHEN** user specifies --focus "runtime.*"
- **THEN** system only includes functions matching the regex pattern in analysis

#### Scenario: Ignore specific functions
- **WHEN** user specifies --ignore "runtime.*"
- **THEN** system excludes functions matching the regex pattern from analysis

#### Scenario: Combine focus and ignore
- **WHEN** user specifies both --focus and --ignore patterns
- **THEN** system first applies focus filter, then applies ignore filter to results

### Requirement: Generate call stack paths
The system SHALL extract and format call stack paths showing the sequence of function calls leading to hotspots.

#### Scenario: Default call depth
- **WHEN** user runs analyze without specifying --call-depth
- **THEN** system outputs call stacks up to 5 levels deep with percentage contributions

#### Scenario: Custom call depth
- **WHEN** user specifies --call-depth 10
- **THEN** system outputs call stacks up to 10 levels deep

#### Scenario: Format path output
- **WHEN** system outputs call paths
- **THEN** each path is formatted as "func1;func2;func3 percentage%" on a single line

### Requirement: Generate natural language summary
The system SHALL generate a concise natural language summary of the performance characteristics.

#### Scenario: CPU profile summary
- **WHEN** analyzing a CPU profile
- **THEN** summary identifies main CPU bottlenecks (e.g., "CPU 热点集中在 JSON 序列化与内存分配")

#### Scenario: Heap profile summary
- **WHEN** analyzing a heap profile
- **THEN** summary identifies main memory allocation patterns and top allocators

#### Scenario: Goroutine profile summary
- **WHEN** analyzing a goroutine profile
- **THEN** summary identifies goroutine leak patterns and blocking calls

### Requirement: Output in multiple formats
The system SHALL support text, JSON, and markdown output formats with consistent data structure.

#### Scenario: Text output by default
- **WHEN** user runs analyze without --format flag
- **THEN** system outputs formatted text with aligned columns and headers

#### Scenario: JSON output
- **WHEN** user specifies --format json
- **THEN** system outputs valid JSON with structure: {type, duration, samples, sample_types, top[], paths[], summary}

#### Scenario: JSON output with symbol information
- **WHEN** profile contains function symbols and --format json is specified
- **THEN** JSON output includes function, file, flat, flat%, cum, cum% for each hotspot

#### Scenario: JSON output without symbol information  
- **WHEN** profile lacks function symbols and --format json is specified
- **THEN** JSON output includes location_id, address, module (from mapping), flat, flat%, cum, cum%
- **AND** function and file fields are set to null

#### Scenario: Markdown output
- **WHEN** user specifies --format markdown
- **THEN** system outputs formatted markdown with tables for hotspots and code blocks for paths

### Requirement: Output collapsed stack format optionally
The system SHALL optionally output collapsed stack format compatible with flame graph generation tools.

#### Scenario: Include collapsed stacks
- **WHEN** user specifies --collapse flag
- **THEN** system outputs both standard analysis and collapsed stack format

#### Scenario: Collapsed stack format
- **WHEN** generating collapsed stacks
- **THEN** each line is formatted as "func1;func2;func3 count" suitable for flamegraph tools

### Requirement: Report profile metadata
The system SHALL extract and report key metadata from the profile.

#### Scenario: Report duration for CPU profiles
- **WHEN** analyzing a CPU profile with duration information
- **THEN** output includes the profile duration in human-readable format (e.g., "30.12s")

#### Scenario: Report sample count
- **WHEN** analyzing any profile type
- **THEN** output includes the total number of samples in the profile

### Requirement: Value-type flag must actually change analysis output
The system MUST honor the `--value-type` flag by analyzing samples using the user-specified value type instead of falling back to the default.

#### Scenario: Different value types produce different hotspot rankings
- **WHEN** user runs `agent-insight analyze heap.pb.gz --value-type alloc_objects` and again with `--value-type inuse_space`
- **THEN** the two outputs show different flat values for the same hotspots (because alloc_objects counts objects while inuse_space counts bytes)
- **AND** the JSON output's `analyzed_type` field reflects the user-specified value type

#### Scenario: Invalid value-type returns clear error
- **WHEN** user runs `agent-insight analyze heap.pb.gz --value-type nonexistent_metric`
- **THEN** system exits with non-zero status
- **AND** error message lists the available value types from the profile

### Requirement: Profile type fallback when PeriodType is missing
The system MUST infer profile type from `SampleType` when `PeriodType` is nil or its type is empty, instead of returning an empty string.

#### Scenario: Heap profile with missing PeriodType reports heap
- **WHEN** user analyzes a heap profile where `PeriodType` is nil but `SampleType` contains `inuse_space` and `alloc_objects`
- **THEN** JSON output's `type` field is `heap` (not empty string)

#### Scenario: Goroutine profile reports goroutine
- **WHEN** user analyzes a goroutine profile
- **THEN** JSON output's `type` field is `goroutine`

#### Scenario: Unrecognized profile reports unknown
- **WHEN** user analyzes a profile with SampleType that contains no recognizable keywords
- **THEN** JSON output's `type` field is `unknown` (not empty string)

#### Scenario: PeriodType present still wins
- **WHEN** user analyzes a profile with `PeriodType.Type = "cpu"` and `SampleType` containing `inuse_space`
- **THEN** JSON output's `type` field is `cpu` (PeriodType takes precedence over inference)

### Requirement: Mapping file paths are normalized to basename
The system SHALL normalize mapping `File` fields to `filepath.Base()` form so that the same binary produces consistent output across profiles collected at different times or locations.

#### Scenario: Absolute path is reduced to basename
- **WHEN** profile mapping has `File = "/home/user/cppbench_server_main"`
- **THEN** system outputs `cppbench_server_main`

#### Scenario: Bracketed identifiers are preserved
- **WHEN** profile mapping has `File = "[vdso]"` or `"[vsyscall]"`
- **THEN** system outputs the same bracketed identifier (unchanged)

#### Scenario: Empty file field stays empty
- **WHEN** profile mapping has empty `File`
- **THEN** system outputs empty string (no placeholder)

#### Scenario: Module field in analyze hotspot uses normalized file
- **WHEN** analyze output includes `module` field for a hotspot without symbols
- **THEN** `module` value is the basename of the mapping file

#### Scenario: Mappings list in info output uses normalized file
- **WHEN** info command outputs `mappings[]` array
- **THEN** each `file` value is the basename of the original mapping file

### Requirement: Analysis summary adapts to profile type
系统 SHALL 在 summary 中使用与 profile 类型匹配的措辞。

#### Scenario: CPU profile summary
- **WHEN** 分析一个 cpu 类型的 profile
- **THEN** summary 使用 CPU 相关措辞（如 "CPU hotspot"、"性能热点"）

#### Scenario: Heap profile summary
- **WHEN** 分析一个 heap 类型的 profile
- **THEN** summary 使用内存相关措辞（如 "memory hotspot"、"内存热点"）

#### Scenario: Goroutine profile summary
- **WHEN** 分析一个 goroutine 类型的 profile
- **THEN** summary 使用 goroutine 相关措辞（如 "blocking point"、"阻塞点"）

### Requirement: JSON output includes value unit
系统 SHALL 在 JSON 输出的 flat/cum 值旁附带单位信息。

#### Scenario: Heap profile JSON output
- **WHEN** 分析 heap profile 并输出 JSON
- **THEN** 输出中包含 unit 字段（如 "bytes"）

#### Scenario: CPU profile JSON output
- **WHEN** 分析 cpu profile 并输出 JSON
- **THEN** 输出中包含 unit 字段（如 "nanoseconds" 或 "count"）

### Requirement: JSON percentage precision
系统 SHALL 在 JSON 输出中统一百分比字段保留 2 位小数。

#### Scenario: Percentage formatting
- **WHEN** 输出 flat_percent 和 cum_percent 字段
- **THEN** 值统一为 2 位小数（如 66.67 而非 66.66666666666666）

### Requirement: JSON field naming convention
系统 SHALL 在 JSON 输出中统一使用 snake_case 字段命名。

#### Scenario: Field naming consistency
- **WHEN** 输出 JSON 格式
- **THEN** 所有字段名使用 snake_case（如 flat_percent 而非 flatPercent）

### Requirement: calculateHotspots 除零防护
`calculateHotspots` SHALL 在 totalValue == 0 时返回空 hotspots 切片而非产生 NaN 百分比。

#### Scenario: 所有 sample 被 focus/ignore 过滤
- **WHEN** profile 有样本但所有样本被 focus/ignore 模式过滤掉
- **THEN** `calculateHotspots` 返回空切片，不报错，不产生 NaN

#### Scenario: 正常 profile 不受影响
- **WHEN** profile 包含样本且无过滤或过滤后仍有样本
- **THEN** 行为与修改前完全一致


### Requirement: Flame command format consistency
flame 命令的 JSON 输出 SHALL 使用 `count` 字段名表示采样值，与 text 格式输出和 profile 层的 `FoldedStack.Count` 字段保持一致。当 `--stats` flag 与非 text 格式同时使用时，系统 SHALL 向 stderr 输出警告信息但不中断执行。

#### Scenario: JSON 输出字段名一致
- **WHEN** 用户以 `--format json` 运行 flame 命令
- **THEN** JSON 输出使用 `count` 字段而非 `value`

#### Scenario: --stats 与 json 格式
- **WHEN** 用户使用 `--stats --format json` 运行 flame 命令
- **THEN** 系统向 stderr 输出警告 "stats output is only supported in text format" 但继续正常输出 JSON

#### Scenario: --stats 与 text 格式
- **WHEN** 用户使用 `--stats --format text` 运行 flame 命令
- **THEN** 系统正常输出 stats 信息，无警告
