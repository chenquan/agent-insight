## 1. 更新触发条件

- [x] 1.1 将触发表中 diagnose 的触发场景从"用户需要 AI 辅助性能诊断"改为"用户不确定问题在哪，需要全面概览"（如"帮我看看"、"诊断一下"但没有具体焦点）
- [x] 1.2 更新决策树中 diagnose 的触发行，从"用户需要完整的性能诊断报告"改为更具体的探索性场景描述

## 2. 添加 diagnose 使用指引

- [x] 2.1 在 diagnose 命令说明段落后添加"何时用 / 何时不该用"对比段落，列出应该用其他命令的具体场景（指定函数 → analyze+list、对比 → diff、内存泄漏 → analyze --value-type、火焰图 → flame 等）
- [x] 2.2 在快速概览工作流的决策点中增加"不确定问题"分支，指向 `diagnose`

## 3. 验证

- [x] 3.1 运行 `make build` 确认构建通过（确保模板文件无语法问题）
- [x] 3.2 运行 `make test` 确认测试通过
